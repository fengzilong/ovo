import XML from './xml';
import If from './if';
import Each from './each';
import Slot from './slot';
import Expression from './expression';

export default {
	XML,
	If,
	Each,
	Slot,
	Expression,
};
